// spaceName()

// How can we declare a function?
// Function expression 
const anyNameOfTheFunction = function (parameters) {
  // instructions
  return 'something'
}
// Function expression
const superArrowFunction = (parameters) => {
  // instructions
  return something
}
// Function declaration
function spaceName(parameters) {
  // instructions
  return something
}

// Arrow functions!

const superArrowFunction = (parameters) => {
  // instructions
  return something
}

const superArrowFunction = parameters => {
  // instructions
  return something
}

const superArrowFunction = (parameters, param2) => {
  // instructions
  return something
}

const addTwoToParam = (param) => {
  return param + 2
}

const addTwoToParam = (param) => param + 2

  // What's an anonymous function?

  (param) => param + 2

console.log(2 + 2)

console.log(param => param + 2)

